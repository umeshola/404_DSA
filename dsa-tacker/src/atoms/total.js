import { atom } from "recoil";
export const totall = atom({
    key: 'totall', // unique ID (with respect to other atoms/selectors)
    default: 0,
});
export const totala = atom({
    key: 'totala', // unique ID (with respect to other atoms/selectors)
    default: 0,
});
export const totalg = atom({
    key: 'totalg', // unique ID (with respect to other atoms/selectors)
    default: 0,
});
export const totalt = atom({
    key: 'totalt', // unique ID (with respect to other atoms/selectors)
    default: 0,
});
export const totaladv = atom({
    key: 'totaladv', // unique ID (with respect to other atoms/selectors)
    default: 0,
});